apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: MyProject
  template:
    metadata:
      labels:
        app: MyProject
    spec:
      containers:
      - name: MyProject
        image: rohit19aug/myproject
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5000
